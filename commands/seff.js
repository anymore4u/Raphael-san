module.exports = {
    name: 'seff',
    description: 'Menciona o usuário e envia uma mensagem.',
    async execute(interaction) {
        await interaction.reply("o <@713399263157026857> é calvo!");
    }
}
